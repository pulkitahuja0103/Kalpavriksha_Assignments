Create 2 single linked list in which cycle is present.
now your task is to remove that cycle , sort the lists and then merge them .
1 <= list.length <= 10^5
-10^4 <= list.val <= 10^4

program will handle all edge cases like invalid linked list size , invalid cycle position.